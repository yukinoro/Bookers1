<body>
	<div class="notice">
	<% if flash[:notice] %>
  <p><%= flash[:notice] %></p>
<% end %>
		
	</div>
	<p>
		<strong>Title:</strong>
		<%= @blog.title %>
	</p>
	<p>
		<strong>Body:</strong>
		<%= @blog.body %>
	</p>
	<%= link_to "Edit", edit_blog_path(@blog) ,class: "edit_#{@blog.id}" %>
	<a>|</a>
	<%= link_to "Back", blogs_path, class: "back" %>
</body>